// key-value pairs for all the API errors

const errorDict = {
    email_taken: "an account with this email already exists",
    generic: "an error has occurred, try again"
}

export default errorDict